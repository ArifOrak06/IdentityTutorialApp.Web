@model RoleCreateViewModel
@{
    ViewData["Title"] = "Role Ekle"; 
}

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}



<div class="row">
    <div class="col-md-6 offset-md-3">
        <h3>Role Ekleme Formu</h3>
        <hr />
        <div>
            @if(TempData["SuccessMessage"] != null)
            {
                <div class="alert alert-info" role="alert">@TempData["SuccessMessage"]</div>
            }
        </div>
        <div asp-validation-summary="ModelOnly"></div>
        <form asp-action="RoleCreate" method="post">
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input type="text" class="form-control" asp-for="Name" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group mt-2">
                <button type="submit" class="btn btn-outline-success btn-sm">Ekle</button>
                <a class="btn btn-outline-primary" asp-action="Index">Role Listesine Dön</a>
            </div>
        </form>
        
    </div>
</div>